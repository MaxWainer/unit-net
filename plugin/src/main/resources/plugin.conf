info {
  main = 'com.example.plugin.MyMainClass' # Path to your main class
  version = '1.0'                         # Plugin version (Optional, may be defined in annotation)
  description = [                         # Plugin description (Optional, may be defined in annotation)
    'My description'
  ]
  authors = [
    'Max_Wainer'
  ]
}

# (Optional, may be defined in annotation)
# You also can implement dependencies what you need
# and they will be loaded in libs folder (Which holds in server local folder)
dependencies = [
  '',
  '',
  ''
]